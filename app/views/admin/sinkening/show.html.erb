<h1 style="font-size: 2rem;">The Sinkening Control Panel</h1>
<p style="color: var(--color-muted);">Control the sinkening intensity across the site</p>

<div class="asec" style="max-width: 600px; padding: 2em;">
  <h2>Current Intensity</h2>
  <p>Current intensity: <strong><%= @setting.intensity %></strong></p>

  <%= form_with url: admin_sinkening_path, method: :patch, local: true, style: "margin-top: 2em;" do |form| %>
    <div style="margin-bottom: 1.5em;">
      <%= form.label :intensity, "Intensity (0.0 - 10.0):", style: "display: block; margin-bottom: 0.5em; font-weight: 600;" %>
      <%= form.number_field :intensity,
            value: @setting.intensity,
            step: 0.1,
            min: 0,
            max: 10,
            style: "width: 200px; padding: 0.5em; border: 1px solid #ccc; border-radius: 4px; font-size: 1rem;" %>
    </div>

    <div style="margin-bottom: 1em;">
      <small style="color: var(--color-muted);">
        • 0 = No sinkening effect<br>
        • 0.3 = Subtle effect (default)<br>
        • 1.0 = Normal intensity<br>
        • 5.0+ = Heavy sinkening<br>
        • 10.0 = Maximum chaos
      </small>
    </div>

    <%= form.submit "Update Intensity", class: "abtn primary", style: "margin-top: 1em;" %>
  <% end %>
</div>

<div class="asec" style="max-width: 600px; padding: 2em; margin-top: 2em;">
  <h2>About The Sinkening</h2>
  <p>The Sinkening adds visual water effects and tilting animations to the site when enabled for users via the <code>:sinkening</code> flipper flag.</p>

  <h3>Effects controlled by intensity:</h3>
  <ul>
    <li>Water wave height and movement</li>
    <li>Container rotation and vertical movement</li>
    <li>Animation amplification</li>
  </ul>

  <p><%= link_to "Manage Flipper Flags", "/admin/flipper", class: "abtn secondary small" %></p>
</div>
