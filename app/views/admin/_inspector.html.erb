<% if (params[:inspect] || Rails.env.development? || defined(always_inspect)) && (current_user&.is_admin? || (defined?(show_in_impersonate) && current_impersonator&.is_admin?)) %>
<details style="border: 1px solid deeppink; display: inline-block">
  <summary style="font-family: 'Comic Sans MS'"><%= record.model_name.human %> record #<%= record.id %></summary>
  <%== ap record %>
</details>
<% end %>
