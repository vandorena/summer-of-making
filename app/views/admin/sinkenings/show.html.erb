<h1 style="font-size: 2rem;">the sinkening (noooo)</h1>

<div class="asec" style="max-width: 600px; padding: 2em;">
  <h2>intensity</h2>
  <p>how doomed are we? (water & tilt)<br>current intensity: <strong><%= @setting.intensity %></strong></p>

  <%= form_with url: admin_sinkening_path(@setting), method: :patch, local: true, style: "margin-top: 2em;" do |form| %>
    <div style="margin-bottom: 1.5em;">
      <%= form.label :intensity, "intensity (0.0 - 1.0):", style: "display: block; margin-bottom: 0.5em; font-weight: 600;" %>
      <%= form.number_field :intensity,
            value: @setting.intensity,
            step: 0.01,
            min: 0,
            max: 1,
            style: "width: 200px; padding: 0.5em; border: 1px solid #ccc; border-radius: 4px; font-size: 1rem;" %>
    </div>

    <div style="margin-bottom: 1.5em;">
      <%= form.label :slack_story_url, "Slack story URL:", style: "display: block; margin-bottom: 0.5em; font-weight: 600;" %>
      <%= form.url_field :slack_story_url,
            value: @setting.slack_story_url,
            placeholder: "https://hackclub.slack.com/archives/...",
            style: "width: 100%; max-width: 540px; padding: 0.5em; border: 1px solid #ccc; border-radius: 4px; font-size: 1rem;" %>
      <p style="margin-top: 0.5em; color: #666; font-size: 0.9rem;">If blank, we'll fall back to default.</p>
    </div>

    <%= form.submit "Update Intensity", class: "abtn primary", style: "margin-top: 1em;" %>
      <p><%= link_to "Manage Flipper Flag", "/admin/flipper/features/sinkening", class: "abtn secondary small" %></p>
  <% end %>
</div>

<%= link_to "← back", admin_root_path, class: 'aout' %>
