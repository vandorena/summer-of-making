<h2 class="mb-2">Welcome aboard ✨</h2>
<p>Check your inbox (<%= @response_data["email"] %>) to confirm your address.</p>
<% if @response_data %>
  <div class="mt-4">
    <p class="text-sm opacity-75">Status: <%= @response_data['ok'] ? 'Success' : 'Error' %></p>
    <% if @response_data['error'] %>
      <p class="text-red-500">Error: <%= @response_data['error'] %></p>
    <% end %>
  </div>
<% end %>
<p>While you wait...</p>

<%= video_tag "wat.mp4", controls: true %>

<button class="btn" onclick="document.getElementById('signup-modal').close()">Close</button>
