<div class="fixed inset-0 z-[9999] bg-black/50 hidden"
     id="signup-wizard"
     data-controller="signup-wizard"
     data-signup-wizard-target="modal"
     data-action="click->signup-wizard#closeOnOutsideClick">

  <div class="flex items-center justify-center w-full h-full p-4">
    <div class="bg-[#F3ECD8] w-[90vw] h-[90vh] max-w-6xl max-h-[90vh] flex flex-col relative overflow-hidden rounded-2xl shadow-2xl border-4 border-[#E4DCC6]"
         data-signup-wizard-target="container">

    <div class="p-2 border-b border-saddle-taupe border-opacity-20">
      <div class="flex justify-between items-center">
        <div>
          <h2 class="text-2xl font-bold text-forest">Welcome to Summer of Making!</h2>
          <p class="text-saddle-taupe mt-1">Your invitation is on its way! Let's show you how it works:</p>
        </div>
        <button type="button" class="text-gray-600 hover:text-vintage-red cursor-pointer transition-colors duration-200"
                data-action="click->signup-wizard#close">
          <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
    </div>

    <div class="flex-1 p-8 space-y-6 overflow-y-auto">
      <div data-signup-wizard-target="videoContainer" class="text-center space-y-6">
        <div class="space-y-2">
          <p class="text-saddle-taupe max-w-2xl mx-auto">
            We've sent you a Slack invitation! While it's on its way, let's quickly show you the ropes and get you ready to start making!
          </p>
        </div>

        <div class="p-2 max-w-3xl mx-auto">
          <div class="aspect-video bg-gray-800 rounded-lg flex items-center justify-center text-white relative">
            <%= video_tag "count.mp4",
                         controls: true,
                         autoplay: false,
                         muted: false,
                         preload: "auto",
                         playsinline: true,
                         class: "w-full h-full rounded-lg",
                         data: {
                           signup_wizard_target: "introVideo",
                           action: "ended->signup-wizard#videoEnded"
                         } %>
          </div>
          <p class="text-sm text-gray-600 mt-3 italic">After the video ends, we'll remind you to check your email!</p>
        </div>
      </div>
    </div>

    </div>
  </div>
</div>
