<div class="fixed inset-0 z-[9999] bg-black/50 hidden"
     id="signup-wizard"
     data-controller="signup-wizard"
     data-signup-wizard-target="modal"
     data-action="click->signup-wizard#closeOnOutsideClick"
     data-signup-wizard-email-value="">
  <div class="flex items-center justify-center w-full h-full p-4">
    <div class="bg-[#F3ECD8] w-[75vw] max-w-3xl flex flex-col relative overflow-hidden rounded-2xl shadow-2xl border-4 border-[#E4DCC6]"
         data-signup-wizard-target="container">

    <button type="button" class="absolute top-4 right-4 text-gray-600 hover:text-vintage-red cursor-not-allowed transition-colors duration-200 z-20"
            data-action="click->signup-wizard#close"
            data-signup-wizard-target="closeButton"
            disabled>
      <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>

    <div class="flex-1 p-8 space-y-6 overflow-y-auto">
      <div data-signup-wizard-target="videoContainer" class="text-center space-y-2">
        <div class="space-y-2 flex flex-col items-center">
          <h3 class="text-center text-2xl font-bold">
            Invitation sent! You're on your way…
          </h3>
          <p class="text-saddle-taupe text-left max-w-2xl mx-auto">
            <span class="font-extrabold">1. Join Slack</span><span class="font-medium italic"> ← YOU ARE HERE</span><br>
            <span class="font-medium">2. Set up your account</span><br>
            <span class="font-medium">3. Start building</span>
          </p>
          <p id="signup-wizard-generic-message" class="text-gray-900 text-left max-w-2xl mx-auto">
            Here's what's next:
          </p>
        </div>

        <div class="p-2 max-w-3xl mx-auto">
          <div class="aspect-video bg-gray-800 rounded-lg flex items-center justify-center text-white relative">
            <%= video_tag "count.mp4",
                         controls: true,
                         autoplay: false,
                         muted: false,
                         preload: "auto",
                         playsinline: true,
                         loop: true,
                         class: "w-full h-full rounded-lg",
                         data: {
                           signup_wizard_target: "introVideo"
                         } %>
          </div>
        </div>
      </div>
    </div>

    </div>
  </div>
</div>
