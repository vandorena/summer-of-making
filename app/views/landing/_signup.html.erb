<div data-controller="signup-form" class="font-dynapuff w-full max-w-sm ml-0">
  <div class="space-y-4">
    <div class="relative">
      <%= image_tag "email.svg", alt: "Email", class: "absolute left-4 top-1/2 transform -translate-y-1/2 w-5 h-5 pointer-events-none z-10 opacity-60 color[#d4c4b0]" %>
      <input type="email"
             required
             placeholder="Enter your email..."
             autocomplete="email"
             class="marble-textarea with-icon w-full"
             data-signup-form-target="emailInput">
    </div>
    <button type="button"
            class="marble-button w-full text-center"
            data-action="click->signup-form#startWizard">
      Start creating
    </button>
  </div>
</div>
