<div id="tutorial-completion-modal" class="fixed inset-0 z-50 overflow-auto bg-black/50 flex items-center justify-center p-2 md:p-4" data-controller="modal">
  <div class="bg-[#F3ECD8] rounded-2xl border-4 border-[#E4DCC6] w-full max-w-2xl mx-2 md:mx-auto relative max-h-[90vh] flex flex-col overflow-hidden" data-modal-target="container">
    <div class="p-6 md:p-8 text-center">
      <h2 class="text-2xl md:text-3xl mb-4 text-[#4A2D24]">
        Congratulations!
      </h2>

      <p class="text-lg md:text-xl mb-6 text-[#6B5B4F] leading-relaxed">
        You've successfully set up your account!
        The platform is now unlocked and you can check out the tutorial!
      </p>

      <%= link_to campfire_path(tutorial_completed: true),
                  class: "inline-block px-8 py-4 bg-gradient-to-r from-[#8b6f47] to-[#a0845c] text-white rounded-xl text-lg md:text-xl font-semibold hover:scale-105 transition-transform duration-200 shadow-lg" do %>
        Let's Go to Campfire!
      <% end %>
    </div>
  </div>
</div>
